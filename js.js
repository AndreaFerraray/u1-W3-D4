const estrai = function () {
  const number = Math.round(Math.random() * 76);
  console.log(number);
};
estrai();
const cell = [];
const numeri = function () {
  for (let i = 0; i < 76; i++);
  cell.push = [];
};
numeri();
